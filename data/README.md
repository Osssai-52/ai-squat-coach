# data/

영상·라벨 데이터 폴더. **git에 올라가지 않음** (.gitignore 처리).

- `videos/` — 공유 드라이브에서 내려받은 촬영 영상 (파일명 규칙: `이름_클래스_회차.mp4`)
- `features.csv` — `ml/extract_features.py`로 생성한 학습용 피처

촬영 규칙은 [docs/recording-rules.md](../docs/recording-rules.md) 참고.
